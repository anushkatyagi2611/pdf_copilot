# PDF parsing & OCR
pymupdf             # for reading PDF text
pillow              # image support
paddleocr>=2.7.0    # SOTA OCR engine

# HTTP & env helpers
requests
python-dotenv       # load HF_API_TOKEN

# (Optional: for local testing without HF) 
# transformers
# sentencepiece

